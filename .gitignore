node_modules
dist
.env
.env.local
.DS_Store

# Configuração pessoal do Claude Code (por desenvolvedor)
.claude/settings.local.json

# Cópia local do app (repo separado — não rastrear aqui)
app/
start-app.cjs
